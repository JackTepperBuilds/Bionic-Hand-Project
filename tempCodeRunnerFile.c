#include <stdio.h>

void printTriangle(int n);

int main() {
    void printTriangle(int n);
    int main() {
    printTriangle(4);
    printTriangle(8);
    return 0;
}

void printTriangle(int n) {
    int x, y;

    for (x = 0; x < n; x++) {
        printf(" ");
        for (y = 0; y <= n*n; y++) {
            
        }
    }
}